<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width,initial-scale=1.0" />
    <link rel="stylesheet" href="/css/app.css" />
    <title></title>
  </head>

  <body>
    <h1>Edit a fruit!</h1>
    <!--  NOTE: the form is prepopulated with values from the server-->
    <div class="back">
      <form action="/fruits/<%= fruit.id%>?_method=PUT" method="POST">
        Name: <input type="text" name="name" value="<%=fruit.name %>" /><br />
        Color: <input type="text" name="color" value="<%=fruit.color %>" /><br />
        Is Ready To Eat: <input type="checkbox" name="readyToEat" <% if(fruit.readyToEat === true){ %> checked <% } %>/>
        <select name="season">
          <% for ( let i = 0; i < seasons.length; i++ ) { let selected = ( i == 0
          ) ? "selected" : ""; %>
          <option value="<%=seasons[i].id%>" selected
            ><%=seasons[i].name%>
          </option>
          <% } %>
        </select>
        <input type="submit" name="" value="Submit Changes" />
      </form>
    </div>
  </body>
</html>
